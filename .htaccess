RewriteEngine On
Options -Indexes

# Si es un archivo existente (incluyendo assets, uploads), servirlo directamente
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Todo lo demás pasarlo a controllers/route.php
RewriteRule ^(.*)$ controllers/route.php [QSA,L]
